<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="actionBarTheme">@style/ThemeOverlay.AppTheme.ActionBar</item>
    <item name="colorPrimary">@color/orangeApplied</item>
    <item name="colorPrimaryDark">@color/orangeLight</item>
    <item name="colorAccent">@color/colorAccent</item>
</style>
    <style name="ThemeOverlay.AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- title text color -->
        <!--<item name="android:textColorPrimary">@color/blak</item>-->
        <item name="android:textColorPrimary">@color/blak</item>
        <!-- subtitle text color -->
        <!--<item name="android:textColorSecondary">@color/blak</item>-->
        <!-- action menu item text color -->
        <!--<item name="actionMenuTextColor">@color/blak</item>-->
        <!-- action menu item icon color -->
        <!--<item name="colorControlNormal">@color/blak</item>-->
    </style>
</resources>
